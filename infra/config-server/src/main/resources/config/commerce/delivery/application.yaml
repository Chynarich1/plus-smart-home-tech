spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/delivery
    username: dbuser
    password: 12345
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true

delivery:
  service:
    base:
      cost: 5.0
    double:
      address:
        name: "ADDRESS_2"